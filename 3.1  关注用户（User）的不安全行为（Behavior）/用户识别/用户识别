在用户的不安全行为识别时，我们将用户分为两大类，

* 管理员用户
* 普通用户
  * 内部用户（主要指企业员工）
  * 外部用户（企业所服务的对象）

这两类用户在信息系统使用中分别对应前台和后台，风险的关注点也有所不同，我们将其分开；

再将普通用户细分为内部用户和外部用户两类，内部用户主要是企业自己的员工，外部用户就是企业所服务的对象，这两类用户所接触到的数据和信息的重要性，敏感程度不尽相同，我们也将进行划分。
